name: cashew
description: budgeting your monthly nut

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0+2

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^6.0.2
  shared_preferences: ^2.0.13
  auto_size_text: ^3.0.0
  package_info_plus: ^5.0.1
  path_provider: ^2.0.9
  url_launcher: ^6.0.20
  hive: ^2.1.0
  hive_flutter: ^1.1.0
  font_awesome_flutter: ^10.1.0
  google_fonts: ^6.1.0
  fl_chart: ^0.66.2
  jiffy: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  hive_generator: ^2.0.1
  build_runner: ^2.1.8

flutter:
  uses-material-design: true
